const Alert = () => {
    return (
        <div style={bg}>
            {Props.text}    
        </div>
    )
}

const bg = {
    background: "#",
    padding: '20px',
    borderRadius: '10px'
}

export default Alert